ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        host = "0.0.0.0"
    }
    application {
        modules = [com.example.bakendorderwhatsapp.ApplicationKt.module]
    }
}

database {
    # Default: Render external host (local). On Render Web Service use internal URL via DATABASE_URL.
    url = "jdbc:postgresql://dpg-dak5qbid0e5s73b404e0-a.oregon-postgres.render.com:5432/orderwhatsapp"
    url = ${?DATABASE_URL}
    user = "orderwhatsapp_user"
    user = ${?DATABASE_USER}
    password = ${DATABASE_PASSWORD}
    driver = "org.postgresql.Driver"
    maxPoolSize = "10"
}
