ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        host = "0.0.0.0"
    }
    application {
        modules = [com.example.bakendorderwhatsapp.ApplicationKt.module]
    }
}

database {
    # Prefer setting DATABASE_URL on Render (internal hostname).
    url = "jdbc:postgresql://dpg-dak5qbid0e5s73b404e0-a:5432/orderwhatsapp?sslmode=require"
    url = ${?DATABASE_URL}
    user = "orderwhatsapp_user"
    user = ${?DATABASE_USER}
    password = ""
    password = ${?DATABASE_PASSWORD}
    maxPoolSize = "5"
}
