ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        host = "0.0.0.0"
    }
    application {
        modules = [com.example.bakendorderwhatsapp.ApplicationKt.module]
    }
}

database {
    url = "jdbc:postgresql://dpg-dak5qbid0e5s73b404e0-a:5432/orderwhatsapp"
    url = ${?DATABASE_URL}
    user = "orderwhatsapp_user"
    user = ${?DATABASE_USER}
    password = ""
    password = ${?DATABASE_PASSWORD}
    driver = "org.postgresql.Driver"
    maxPoolSize = "10"
}
